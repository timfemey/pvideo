import { FunctionalComponent, h } from "preact";
import "./header.css";
const Header: FunctionalComponent = () => {
  return (
    <header className="header">
      <h1 className="logo">Pvideo :)</h1>
    </header>
  );
};

export default Header;
